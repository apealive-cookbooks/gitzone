#source "https://api.berkshelf.com"

metadata

# Following cookbook were applied during testing before gitzone is deployed
# cookbook "bind", :git => "http://github.com/atomic-penguin/cookbook-bind"
# cookbook "hostsfile"
